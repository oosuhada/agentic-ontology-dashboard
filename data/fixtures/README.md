# Gold Fixtures

The eight `GS-*.json` scenarios are the explicit fallback dataset for the current manufacturing MVP.

They cover stable, warning, critical, multifactor, low-confidence, invalid-sensor and LLM-offline states. They are not production performance evidence and are shown as a fallback source in the UI.

`maintenance_context.json` contains the checklist and source-reference context used when building Evidence Packages.
