# Palantir UI gap v2 screenshot set

Generated by `web/e2e/palantir-visual-audit.spec.ts` at a fixed 1440×1000 light-theme viewport. `comparison.html` places every local capture beside its official Palantir reference, and `comparison-sheet.png` is the fully rendered long-form review sheet.

| Local capture | Product surface | Primary official comparison |
|---|---|---|
| `dashboard.png` | Role Dashboard | `https://www.palantir.com/docs/resources/foundry/contour/dashboard-overview.png` and `dashboard-creating-a-dashboard.png` |
| `analysis.png` | Analysis Path + Result Inspector Lineage | `https://www.palantir.com/docs/resources/foundry/contour/overview.png` and `boards-verify-table.gif` |
| `agent.png` | Agent Evidence Workbench | Blueprint/Foundry evidence-first three-pane language; compare density and hierarchy rather than feature parity |
| `governance.png` | Governance Workbench | Foundry-style operational registry/table density and status hierarchy |
| `datasets.png` | Dataset Catalog | Contour dataset/version and project-reference patterns |

Automated E2E verifies that each route renders its primary workbench, that the Analysis inspector includes the read-only upstream DAG mini graph, and that all five official reference images load successfully in the comparison page.

## Provisional visual verdict

- Dashboard: strong structural match — global navigation, active scope, parameter rail, governed grid, status and interaction affordances are aligned.
- Analysis: direct product-pattern match — board rail, path canvas, result preview and inspector hierarchy are aligned; the local version adds explicit correctness and lineage metadata.
- Agent: Foundry-language adaptation rather than feature parity — compare evidence hierarchy, density and scoped-permission cues, not the exact authoring workflow.
- Governance: operational pattern match — compact filters, status tags, record list and selected detail follow the same high-density administration language.
- Datasets: strong information-architecture match — identity, metadata, versions, preview and operational health follow Dataset Preview conventions.

The product should be described as **Foundry-inspired**, not a pixel clone. Final aesthetic acceptance remains a human decision after opening `comparison-sheet.png` or `comparison.html` at full size.
