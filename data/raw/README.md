# Raw data staging

Raw Canonical V3.1 source packages may be staged here locally, but large source data is not committed. Use the governed bundle manifest, checksums and ingestion scripts to materialize PostgreSQL runtime data.
